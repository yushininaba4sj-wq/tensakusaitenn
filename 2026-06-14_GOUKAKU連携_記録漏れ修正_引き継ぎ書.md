# GOUKAKU LINK → SENPAI LINK 連携 記録漏れ修正 引き継ぎ書

- 担当部門: 技術担当
- 作成日: 2026-06-14
- 対象リポジトリ: `tensakusaitenn`（GOUKAKU LINK 本体 / goukakulink.vercel.app）
- 受け側: `juku-matching`（SENPAI LINK / senpailink.vercel.app）

> このリポジトリ（tensakusaitenn）は友人運営のため、コミット・デプロイは**友人本人が実施**する。本書は「何を・なぜ・どう直すか」を渡すためのもの。

---

## 1. 症状（事実）

GOUKAKU LINK でサービス（添削・24時間質問・過去問分析）を実行しても、SENPAI LINK の統合運営ダッシュボード（`/admin/dashboard`）の「GOUKAKU依頼 受付済み／要対応」に正しく出ない。

## 2. 原因（事実）

GOUKAKU は `app/api/submissions/route.ts` → `lib/senpaiSync.ts` の同期処理で、SENPAI 側の `student_service_requests` テーブルに**直接 insert** している（API 失敗時のフォールバック含む）。この同期値が**旧仕様**のままで、SENPAI 側の期待値とズレていた。

### 本番(修正前)の旧値 と SENPAI側の期待値

| 項目 | 本番の旧値 | SENPAI が期待する値 |
|------|-----------|--------------------|
| `service_type`（kakomon の場合） | `kakomon` | `kakomon_bunseki` |
| `status` | `pending` | `new` |
| `attachments[].bucket` | `service-attachments` | `service-request-attachments` |

- `status` が `new` でないと、ダッシュボードの「受付済み（status=new）」「要対応」に計上されない（累計には出るが対応キューに乗らない）。
- `service_type` が `kakomon` だとラベルが「依頼」とフォールバックされ、「過去問分析」として扱われない／AI下書き等の分岐（`service_type === "kakomon_bunseki"` 判定）に乗らない。

## 3. コード修正（リポジトリ反映済み・要デプロイ）

```
lib/senpaiSync.ts
  - kakomon: "kakomon"            →  "kakomon_bunseki"
  - bucket:  "service-attachments" →  "service-request-attachments"（SUBMISSION_IMAGE_BUCKET と共有）
  - status:  "pending"            →  "new"

lib/submissionImages.ts
  - SUBMISSION_IMAGE_BUCKET = "service-request-attachments"
  - Bucket not found 時のエラー文言を正しいバケット名に合わせた

supabase/migrations/20260612_service_request_attachments_rls.sql（新規）
supabase/storage.sql（更新）
  - service-attachments バケット新規作成をやめ、既存 service-request-attachments への RLS 追加に変更
```

**友人側:** `git pull` → 本番デプロイ（Vercel）

## 4. Supabase 側で必要な作業（友人 or DB管理者）

**① student_service_requests の INSERT RLS**（実施済みの場合はスキップ）

`supabase/student_service_requests_rls.sql` を SQL Editor で実行。

**② Storage RLS（画像アップロード用）**

`supabase/storage.sql` または `supabase/migrations/20260612_service_request_attachments_rls.sql` を SQL Editor で実行。

- バケット `service-request-attachments` は SENPAI 側既存を使用（新規作成しない）
- GOUKAKU ユーザーが `{user_id}/...` パスで upload / read できるポリシーを追加

**③ 既存データの移行（任意）**

既に `service-attachments` バケットにアップ済みの画像があれば、移行または旧バケット読取ポリシーの維持を判断。

## 5. SENPAI 側（juku-matching）で期待する受け入れスキーマ（受け側仕様の正）

`student_service_requests` に insert する際の値：

- `service_type` ∈ `{ "study_room", "correction", "kakomon_bunseki" }`（`plan` は SENPAI のラベル未定義＝「依頼」表示になる。必要なら別途調整）
- `status` = `"new"`（決済が絡む場合は SENPAI 側が `pending_payment → new` を確定）
- `attachments[]` = `{ url, type:"image", bucket:"service-request-attachments", path, name? }`
- `user_id`, `message` は従来どおり

## 6. SENPAI 側で別途実施済み（参考・本書の対象外）

SENPAI(juku-matching) 側では、Stripe 決済確定を取りこぼさないよう以下を改修済み（コミット/デプロイは別途）:

- `api/stripe/webhook/route.ts`：決済確定で `student_service_requests` を `pending_payment → new` に更新＋Slack(n8n)/LINE 通知（race-safe）
- `student/{correction,study-room,kakomon-bunseki}/complete/page.tsx`：完了ページ側も同じ race-safe 方式＋Slack 通知に統一

※ GOUKAKU の直接同期（本書 3〜4章）は**決済を経由しない無料/即時依頼**の経路なので、webhook 改修だけでは直らない。両方そろって初めて本番不具合が解消する。

## 7. デプロイ後の検証手順

1. GOUKAKU LINK でテスト依頼を1件送信（できれば過去問分析）。
2. SENPAI `/admin/dashboard` で「GOUKAKU依頼 受付済み」がカウントされるか。
3. フィード/依頼一覧で `service_type` が「過去問分析」表示になっているか（`kakomon_bunseki`）。
4. 画像つき依頼で、`attachments[].bucket` が `service-request-attachments` で、実体も同bucketにあるか（Storageで確認）。
5. Slack(n8n) 通知が届くか（SENPAI側 webhook/通知デプロイが前提）。

## 8. 本番インフラ（参考）

| 項目 | 値 |
|------|-----|
| Vercel project | `yushininaba4sj-2351s-projects / tensakusaitenn` |
| 本番 URL | https://goukakulink.vercel.app |
| `OPS_SLACK_WEBHOOK_URL` | Production 設定済み |
| 共有 Supabase | `xmbzpllpjjhaesinlknq.supabase.co` |

---

関連ドキュメント: `supabase/運営通知設定.md`, `supabase/画像アップロード設定.md`
